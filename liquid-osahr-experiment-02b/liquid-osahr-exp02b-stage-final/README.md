# Liquid-OSAHR Experiment 02B

Intervention-calibrated RAN residual over OSAHR. Predictive trust is not
intervention trust: calibration selected mechanistic fallback (α=0);
the confirmatory holdout is query/regime dependent.

```bash
python -m pytest -q
```

Results: `EXPERIMENT_REPORT.md`. Reproduce: `REPRODUCE.md`.
