# Parallel swarm handoff for Mateo

Work in parallel on Seth's Jev-directed Qwen repair experiment inside the existing SyberLabs/OSAHR_Cell GrokCell. Start from the current tip of `experiment/jev-qwen-repair` at `https://github.com/SyberLabs/OSAHR_Cell.git` (at least `28a8c86`). Read `REPAIR_EXPERIMENT.md` and the existing code before changing it. Work in your own checkout and feature branch. Do not edit Seth's shared checkout or push to `experiment/jev-qwen-repair`.

Objective: improve independently accepted complete repairs per total execution dollar without weaker verification, hidden human rescue, or unsafe generated-code execution. Qwen/Qwen3-Coder-Next proposes code; TypeSafe Jev selects bounded legal actions; existing GrokCell state, admission, and independent acceptance remain authoritative. Arm E (Jev-Mem) is blocked because nested provider usage cannot yet be bounded and accounted for. There are no live comparison results: the current machine lacks Docker and provider credentials. Do not infer performance from offline tests.

Assign separate agents these narrow tasks:

1. **Jev-Mem metering:** Inspect the actual Jev-Mem API at the source revision noted in `REPAIR_EXPERIMENT.md`. Implement a real, optional adapter only if every nested provider call can be reserved, capped, and recorded, including input/output usage, time, and cold/warm construction cost. Resolve retrieved IDs to canonical GrokCell records and recheck provenance. If hard caps are impossible, keep E explicitly blocked and report the precise API boundary. Never score an unmetered E run.
2. **Safe restart:** Implement a narrow resume path for interruptions at provider-call, artifact-admission, and result-write boundaries. Preserve spent budget and exact artifact bindings; never double-admit. Treat an interrupted external call as uncertain until reconciled. Add focused offline tests for those crash points.
3. **Independent validity review:** Check A-E arm isolation, equivalent limits, seed-evidence provenance, model/version comparability, and the predeclared improvement threshold. For each material issue, provide a failure mechanism, concrete counterexample, minimal correction, and regression test. Avoid a broad rewrite.

Apply first-principles reasoning and existing GrokCell ownership boundaries. Coordinate file ownership within your swarm. Run only offline tests unless authorized sandbox, provider access, and explicit spending limits are available. Do not execute generated code on the host, bypass acceptance, merge, deploy, or change production settings. Commit and push incremental work to **your own feature branch** as you go; return its URL or a patch, tests actually run, unresolved blockers, and precise integration notes. Distinguish implemented, offline verified, live demonstrated, comparatively measured, and externally validated status.
