# Parallel swarm handoff for Mateo

Work in parallel on Seth's Jev-directed Qwen repair experiment inside the existing SyberLabs/OSAHR_Cell GrokCell. Start from the latest `main` at `https://github.com/SyberLabs/OSAHR_Cell.git`; the experiment was merged in PR #24. Read `REPAIR_EXPERIMENT.md` and the existing code before changing it. Work in your own checkout and feature branch. Do not edit Seth's shared checkout or push directly to `main`.

Objective: improve independently accepted complete repairs per total execution dollar without weaker verification, hidden human rescue, or unsafe generated-code execution. Qwen/Qwen3-Coder-Next proposes code; TypeSafe Jev is an experimental choice policy whose value in this repair loop is unproven. Existing GrokCell state, admission, and independent acceptance remain authoritative. Arm E (Jev-Mem) is blocked because nested provider usage cannot yet be bounded and accounted for. There are no live comparison results: the current machine lacks Docker and provider credentials. Do not infer performance from offline tests.

Assign separate agents these narrow tasks, in this order of evidence value:

1. **Jev fit and validity:** Check whether the current five-task fixture ever offers two productive legal actions to the no-memory Jev arm. It appears to offer none, which would make C identical to deterministic B and confound D with retrieval. Seek real repair incidents with multiple plausible actions and independently observable outcomes; do not manufacture ambiguity. Review arm isolation, equivalent limits, evidence provenance, and model comparability. Return a counterexample and smallest correction for each material flaw. If no suitable decision exists, recommend removing Jev routing from this repair use case.
2. **Safe restart:** Implement a narrow resume path for interruptions at provider-call, artifact-admission, and result-write boundaries. Preserve spent budget and exact artifact bindings; never double-admit. Treat an interrupted external call as uncertain until reconciled. Add focused offline tests for those crash points.
3. **Conditional Jev-Mem research:** Do not implement Jev-Mem until Jev routing has a demonstrated useful decision and simple retrieval has a measured benefit. Then inspect the actual API at the source revision in `REPAIR_EXPERIMENT.md`; integrate only if every nested provider call can be reserved, capped, and recorded, including cold/warm construction cost. Otherwise keep E blocked and report the precise boundary.

Apply first-principles reasoning and existing GrokCell ownership boundaries. Coordinate file ownership within your swarm. Run only offline tests unless authorized sandbox, provider access, and explicit spending limits are available. Do not execute generated code on the host, bypass acceptance, merge, deploy, or change production settings. Commit and push incremental work to **your own feature branch** as you go; return its URL or a patch, tests actually run, unresolved blockers, and precise integration notes. Distinguish implemented, offline verified, live demonstrated, comparatively measured, and externally validated status.
