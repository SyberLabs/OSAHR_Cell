---
concept_id: outage
excludes: []
requires_fidelity: false
---

# Outage

Abnormal-behavior / unavailability of a MEC path. Observed by AnLF
`AbnormalBehavior` and injected only through a `BoundaryHandle`. The analytics
function does not rewrite \(G\).

[[critical]] lists this concept under `excludes`. See also [[load]].
