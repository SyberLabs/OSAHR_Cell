# Liquid-OSAHR Experiment 01

Neural models estimate piecewise-constant event intensities from irregular
telemetry. OSAHR samples the declared CTMC and commits typed DPO rewrites.
The teacher is a synthetic identification testbed, not a calibrated radio.

Seeds: dataset `20260817`, control twin `80219`. Kernel is repository-root `osahr/`.

```bash
python -m pytest -q
```

Results, methods, and limitations: `EXPERIMENT_REPORT.md`. Spec: `EXPERIMENT_SPEC.md`.
